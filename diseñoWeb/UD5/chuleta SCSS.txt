📝 CHULETA RÁPIDA SASS / SCSS
1️⃣ Variables
$color-principal: #333;
$fuente: 'Arial', sans-serif;
$btn-padding: 10px 20px;
$btn-radius: 6px;


Para colores, tamaños, fuentes

Evita hardcodear valores

2️⃣ Mapas (maps)
$card-sizes: (
  "small": 150px,
  "medium": 200px,
  "large": 250px
);

$card-colors: (
  "basic": #bdc3c7,
  "premium": #f1c40f,
  "vip": #9b59b6
);

$alert-colors: (
  "success": #2ecc71,
  "error": #e74c3c
);


Acceso: map-get($mapa, "clave") o map.get($mapa, "clave") si usas @use "sass:map";

Sirve para colores, tamaños, configuraciones

3️⃣ Mixins
// Tema
@mixin theme-light($bg, $color-text, $color-link) {
  background-color: $bg;
  color: $color-text;
  a { color: $color-link; }
}

// Botones
@mixin button-style($bg-color) {
  background-color: $bg-color;
  color: white;
  padding: $btn-padding;
  border-radius: $btn-radius;
  cursor: pointer;

  &:hover { background-color: darken($bg-color, 10%); }
}

// Tarjetas
@mixin card-size($size) {
  height: map.get($card-sizes, $size);
  padding: 20px;
  border-radius: 8px;
}

// Tipografía
@mixin text-base() {
  font-family: $fuente;
  font-size: 16px;
  color: $color-principal;
}

// Alertas
@mixin alert-style($type) {
  $color: map.get($alert-colors, $type);
  background-color: $color;
  color: white;
  padding: 15px;
  margin-top: 10px;
  border-radius: 5px;
}


@include nombre-mixin(parametros); → aplica mixin

&:hover → pseudoclase dentro del mixin

4️⃣ Anidamiento SCSS
nav {
  background-color: #2c3e50;
  ul {
    list-style: none;
    li {
      margin: 0 15px;
      a {
        color: white;
        &:hover { color: #f1c40f; text-decoration: line-through; }
      }
    }
  }
}

.light-theme {
  @include theme-light(#f5f5f5, #333, #3498db);
}


Evita repetir selectores

&:hover se refiere al selector padre

5️⃣ Pseudoclases rápidas
a:hover { color: yellow; }
li + li { margin-left: 10px; } // adyacente
div > p { padding: 1em; }     // hijo directo
div span { color: white; }    // descendiente


+ → hermano adyacente

> → hijo directo

Espacios → descendiente

6️⃣ Funciones Sass útiles
darken($color, 10%);  // oscurecer color
lighten($color, 10%); // aclarar color


Perfecto para hover de botones o alertas

7️⃣ Tips rápidos

Mixins → reutilización

Variables → coherencia de tema

Mapas → configuración centralizada

Anidamiento → código limpio

&:hover y pseudoclases → interactividad

Siempre revisa: unidades correctas (px, em, rem)